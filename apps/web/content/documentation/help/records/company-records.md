---
title: Company records
description: What lives on a company — owner, domains, auto-fetched logo, and the tabs for people, tasks, and notes.
order: 1
updated: "2026-08-13"
related: [help/records/people-records, help/getting-started/create-your-first-company, help/records/see-who-changed-a-record]
---

A company record is the account view: who owns the relationship, the web
domains that identify it, and every person, task, and note attached to it.
Only the name is required — everything else can arrive later.

## The fields

- **Name** — required, and the only built-in column.
- **Account Owner** — the teammate responsible; pick anyone in the workspace.
- **ICP** — a toggle for "matches our ideal customer profile", handy as a
  board and list filter.
- **Domains** — one or more web domains. Domains are unique across companies,
  which is what lets the CSV import match rows to existing accounts.
- **LinkedIn** — the company's profile URL.
- Plus every custom field your workspace has added for companies.

**The logo takes care of itself.** When a company has a domain and no logo
yet, Relaticle fetches the site's icon in the background and keeps it.

![A company record page showing its domains and LinkedIn links, with People, Tasks, Notes, and Activity log tabs underneath](/help-assets/records/company-records-1.png)

## The record page

Open a company to see its details on the left — logo, name, owner, creator,
every visible field — and four tabs:

| Tab | What you can do |
|-----|-----------------|
| People | Add a new person directly into this company |
| Tasks | Create a task pre-linked here, or **Attach** an existing one |
| Notes | Same — create new or attach existing |
| Activity log | A read-only history of who changed what |

The header has **Edit**, and a menu with **Copy page URL**, **Copy record
ID** (useful for [ID-based imports](/help/import/update-existing-records)),
and **Delete record**.

## The list

**Companies** in the sidebar lists every account with sortable columns for
owner, creator, and dates — plus a column for each custom field marked
visible in lists. Filter by **Creation Source** (who or what created the
record — the app, an import, the API, the AI assistant) or show
**Deleted records** to [restore one](/help/records/restore-deleted-records).
Header buttons cover **New company** and **Import / Export**.
