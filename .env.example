APP_NAME=Relaticle
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://localhost

# Laravel Sail Configuration
SAIL_FILES=compose.dev.yml

# System Administrator Panel Configuration
# Use either SYSADMIN_DOMAIN for subdomain or SYSADMIN_PATH for path-based access
# SYSADMIN_DOMAIN=sysadmin.example.com
SYSADMIN_PATH=sysadmin

# Horizon Dashboard Access
# Comma-separated emails allowed to open Horizon outside the local environment.
# Leave empty to deny everyone.
# HORIZON_ADMIN_EMAILS=ops@example.com,admin@example.com

# Application Panel Configuration
# By default, the CRM panel is served at /app (path-based routing)
# For subdomain routing, set APP_PANEL_DOMAIN (e.g., app.example.com)
# APP_PANEL_DOMAIN=app.example.com
# APP_PANEL_PATH=app

# MCP Server Configuration
# MCP_DOMAIN=mcp.relaticle.com

# API Configuration
# API_DOMAIN=api.relaticle.com

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=relaticle
DB_USERNAME=postgres
DB_PASSWORD=postgres

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=reverb
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Queue Configuration
# The application uses separate queues for different job types:
# - default: General application jobs
# - imports: CSV import jobs (handled by dedicated workers with higher timeout/memory)

CACHE_STORE=redis
CACHE_PREFIX=relaticle_cache

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_USERNAME=null
REDIS_PASSWORD=null
REDIS_PORT=6379
BULLMQ_PREFIX=bull

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
RESEND_KEY=
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

CONTACT_EMAIL="hello@relaticle.com"

MAILCOACH_DOMAIN=""
MAILCOACH_API_ENDPOINT=""
MAILCOACH_API_TOKEN=""
MAILCOACH_SUBSCRIBERS_LIST_ID=""
MAILCOACH_ENABLED_SUBSCRIBERS_SYNC=false

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

SENTRY_LARAVEL_DSN=
SENTRY_TRACES_SAMPLE_RATE=1.0

# AI chat (laravel/ai). OpenAI is the default provider (config/ai.php);
# Anthropic powers the Claude models in the picker and the summary model.
# Models only appear in the chat picker when their provider is configured.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Self-hosted AI via Ollama. Set OLLAMA_MODEL to a tool-calling-capable model
# tag (e.g. qwen3:14b) to add it to the chat model picker.
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=

# Generic self-hosted / OpenAI-compatible endpoint (vLLM, LM Studio, LocalAI,
# or a gateway). Set the base URL and a comma-separated model list; each becomes
# a picker entry (id `selfhosted:<tag>`). Key is optional (blank for local servers).
# SELF_HOSTED_AI_URL=http://localhost:8000/v1
# SELF_HOSTED_AI_KEY=
# SELF_HOSTED_AI_MODELS=

# Reverb (broadcasting)
REVERB_APP_ID=
REVERB_APP_KEY=
REVERB_APP_SECRET=
REVERB_HOST="localhost"
REVERB_PORT=8080
REVERB_SCHEME=http
REVERB_ALLOWED_ORIGIN_EXTRA=
REVERB_SCALING_ENABLED=false

# Vite (Echo client)
VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

# Broadcast -> Reverb (server-side HTTP API).
# Most setups: leave these blank -- broadcasts use REVERB_HOST/PORT/SCHEME above.
# Local dev with Herd: set BROADCAST_REVERB_HOST=127.0.0.1 and BROADCAST_REVERB_PORT=8080.
# Scheme + verify auto-default to http + false for loopback hosts.
BROADCAST_REVERB_HOST=
BROADCAST_REVERB_PORT=
BROADCAST_REVERB_SCHEME=
BROADCAST_REVERB_VERIFY=

FATHOM_ANALYTICS_SITE_ID=

# Health Checks (opt-in)
# HEALTH_CHECKS_ENABLED=false
# OH_DEAR_HEALTH_CHECK_SECRET=

# Feature Flags (for forks and custom deployments)
# Set to false to disable specific features
# RELATICLE_FEATURE_ONBOARD_SEED=true
# RELATICLE_FEATURE_SOCIAL_AUTH=true
# RELATICLE_FEATURE_DOCUMENTATION=true
# RELATICLE_FEATURE_SUPPORT_MENU=true
# RELATICLE_FEATURE_BLOG=true

# Community Links
DISCORD_INVITE_URL=https://discord.gg/b9WxzUce4Q

# Cloudflare Turnstile (spam protection)
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Passport OAuth (MCP directory submissions)
# Local dev: run `php artisan passport:keys` once; keys land in storage/ (gitignored).
# Production: either set these env vars inline, or run `php artisan passport:keys --force` on first deploy.
# CI generates fresh keys per run via the workflow itself.
PASSPORT_PRIVATE_KEY=
PASSPORT_PUBLIC_KEY=

# Demo account for Claude/ChatGPT directory reviewers.
# Required by `php artisan db:seed --class=DemoAccountSeeder`; the seeder refuses to run
# without it rather than bake a password into the repository.
DEMO_ACCOUNT_PASSWORD=

# Stripe billing (test-mode keys for local dev). Cashier reads STRIPE_KEY/SECRET/WEBHOOK_SECRET.
STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=
STRIPE_MANAGED_PAYMENTS=true
STRIPE_PRICE_PRO_MONTHLY=
STRIPE_PRICE_PRO_YEARLY=
STRIPE_PRICE_CREDITS_1K=
STRIPE_PRICE_CREDITS_5K=

# Billing feature flag — keep false in production until the Stripe org is live
RELATICLE_FEATURE_BILLING=false

# Help launcher — full URLs of the Maxforms-hosted support forms. Each form
# should carry hidden fields named user_email, user_name, workspace_id,
# source_url, app_version for context to prefill. An unset URL hides its menu
# item; all unset hides the Help section.
SUPPORT_CONTACT_FORM_URL=
SUPPORT_BUG_FORM_URL=
SUPPORT_FEATURE_FORM_URL=
